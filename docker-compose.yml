services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      VISITS_PER_TREE: 3
    volumes:
      # Persist the SQLite database file between container restarts
      - tree-data:/app/data
    restart: unless-stopped

volumes:
  tree-data:
